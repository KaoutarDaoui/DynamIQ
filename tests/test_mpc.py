"""Tests for dynamiq.models.mpc — to be filled in once solve() is
implemented.

Planned cases:
    - solve() never returns setpoints outside [t_min_c, t_max_c].
    - solve() shifts energy use toward hours with lower carbon intensity
      when comfort bounds allow it (pre-cooling behaviour).
    - solve() output trajectory length matches the requested horizon.
"""


def test_placeholder() -> None:
    assert True
